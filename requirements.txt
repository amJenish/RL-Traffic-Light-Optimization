# SUMO binaries (sets SUMO_HOME when package is installed; optional if SUMO is on PATH / env)
eclipse-sumo>=1.26.0

# Core RL pipeline
torch>=2.0.0
numpy>=1.24.0

# Preprocessing  (BuildRoute.py, data_to_rou.py)
pandas>=1.3.0
openpyxl>=3.0.0   # .xlsx support
xlrd>=2.0.0       # .xls support
